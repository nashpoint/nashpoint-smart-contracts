[profile.default]
src = "src"
out = "out"
libs = ["lib"]

evm_version = "cancun"
solc = "0.8.28"
optimizer = true
optimizer-runs = 200
via-ir = false

[profile.deploy]
evm_version = "cancun"
solc = "0.8.28"
optimizer = true
optimizer-runs = 50
via-ir = true


[fuzz]
runs = 10

[etherscan]
mainnet = { key = "${ETHERSCAN_API_KEY}" } 

